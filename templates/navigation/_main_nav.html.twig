 {# 
  Note : Nous passons par un formulaire réalisé en dur car la procédure est plus complexe si Form type dans toutes les vues.
    (cf services, twig tag)
 #}
 <nav class="navbar navbar-light  justify-content-between">

    <form class="form-inline" action="{{ path('faq_index') }}" method="POST">
        <input class="form-control mr-sm-2" type="search" name="title" placeholder="Search" aria-label="Search" minlength=3>

        <button class="btn btn-dark my-2 my-sm-0" type="submit">Search</button>
    </form>

    {#
    
        verifier si un utilisateur est connecté par role ou par etat : https://symfony.com/doc/current/security.html#checking-to-see-if-a-user-is-logged-in-is-authenticated-fully
    #}

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}

        <a class="btn btn-secondary my-2 my-sm-0"  href="{{ path('app_admin')}}">Admin</a>
        

    {% else %}
       
        <a class="btn btn-outline-primary my-2 my-sm-0"  href="{{ path('app_signin')}} ">inscription</a>
    {% endif %}

</nav>