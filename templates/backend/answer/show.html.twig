{% extends 'backend_layout.html.twig' %}

{% block title %}Answer{% endblock %}

{% block content %}
    <h1>Answer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ answer.id }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ answer.author }}</td>
            </tr>
            <tr>
                    <th>question</th>
                    
                </tr>
            <tr>
                <th>Body</th>
                <td>{{ answer.body }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('backend_answer_edit', {'id': answer.id}) }}">edit</a>

    {{ include('backend/answer/_delete_form.html.twig') }}
{% endblock %}
